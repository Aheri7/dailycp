#include <bits/stdc++.h>
using namespace std;
int main()
{
	string s;
	cin>>s;
	int n=s.size();
	int i=0,j=n-1;
	while(i<n){
		if(s[i]==s[j]){
			i++;
			j--;
		}
		else{
			cout<<"NO";
			break;
		}
	}
	if(i==n)
		cout<<"YES";
}
